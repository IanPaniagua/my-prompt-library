---
const nav = [
  { href: '/', label: 'Home' },
  { href: '/prompts', label: 'Prompts Gallery' },
  { href: '/prompt-framework', label: 'Prompt Framework' },
];
---
<header class="border-b border-black/5 bg-white/80 backdrop-blur supports-[backdrop-filter]:bg-white/60">
  <div class="container-tight flex items-center justify-between py-4 min-w-0">
    <a href="/" class="flex items-center gap-2 font-semibold tracking-tight shrink-0">
      <img src="/avatar.png" alt="My Prompt Library logo" class="h-10 w-10 sm:h-12 sm:w-12 rounded-md" />
      <span class="whitespace-nowrap">My Prompt Library</span>
    </a>
    <nav class="ml-4 flex items-center gap-3 sm:gap-6 text-sm overflow-x-auto no-scrollbar whitespace-nowrap">
      {nav.map((n) => (
        <a class="link px-1 py-2 sm:py-0" href={n.href}>{n.label}</a>
      ))}
    </nav>
  </div>
</header>
